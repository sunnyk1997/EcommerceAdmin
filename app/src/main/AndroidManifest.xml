<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.desirestodesigns.ecommerceadmin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.EditAdminDetailsActivity"></activity>
        <activity android:name=".fragment.OrdersTrackingFragment" />
        <activity android:name=".fragment.ProfileFragment" />
        <activity android:name=".fragment.InventoryFragment" />
        <activity android:name=".fragment.Homefragment" />
        <activity android:name=".activity.RegistrationActivity" />
        <activity android:name=".activity.OtpActivity" />
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/MainActivityTheme" />
        <activity android:name=".activity.LoginActivity" />
        <activity
            android:name=".activity.SplashScreenActivity"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>